// Public browser configuration. Keep credentials in ignored .env.local for development.
// Deployed sites may set window.ASTRA_CONFIG before this script runs.
window.ASTRA_CONFIG = window.ASTRA_CONFIG || {};
