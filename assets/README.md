# Astra 3D Asset Pipeline

Set ASSET_BASE_URL in assets/config.js to your public CDN/object-storage origin, for example:
https://assets.example.com/astra/v1

Large GLBs should be hosted there, not in the GitHub repository. Character bases are separate from animation packs. Only configure shared animation packs when skeletons, bone names and rest poses are compatible.